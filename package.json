{
  "name": "zauzoo",
  "version": "1.0.0",
  "description": "Zauzoo front-end app",
  "author": "Jan Zelenka",
  "license": "ISC",
  "repository": "https://github.com/zauzoo/fe",
  "scripts": {
    "build": "rm -Rf build/* && webpack --progress --profile --colors --devtool source-map --optimize-minimize --optimize-occurence-order --optimize-dedupe",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "rm -Rf build/* && webpack-dev-server --hot --inline --progress --colors",
    "lint": "eslint src"
  },
  "devDependencies": {
    "babel-core": "^5.8.20",
    "babel-eslint": "^4.0.5",
    "babel-loader": "^5.3.2",
    "eslint": "^1.0.0",
    "eslint-plugin-react": "^3.2.0",
    "html-webpack-plugin": "^1.6.1",
    "node-libs-browser": "^0.5.2",
    "react-hot-loader": "^1.2.8",
    "webpack": "^1.10.5",
    "webpack-dev-server": "^1.10.1"
  },
  "dependencies": {
    "alt": "^0.17.1",
    "babel-runtime": "^5.8.20",
    "react": "^0.13.3",
    "react-router": "^0.13.3"
  }
}
